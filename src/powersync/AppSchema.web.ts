// Web Fallback for AppSchema
export const AppSchema = {
    // Mock schema object that matches the shape expected by others if needed, 
    // or just a pass-through since we won't strictly use it on web for this demo.
    tables: []
};
